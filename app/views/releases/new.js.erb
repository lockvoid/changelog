var form = document.querySelector('#new_release_form')

if (!form.getAttribute('data-content-was')) {
  form.setAttribute('data-content-was', form.innerHTML);
}

form.innerHTML = '<%=j render "release-form", project: @project, release: @release %>';
