var post = document.querySelector('#<%= dom_id @release, :post %>')


if (!post.getAttribute('data-content-was')) {
  post.setAttribute('data-content-was', post.innerHTML);
}

post.innerHTML = '<%=j render "release-form", release: @release %>';
