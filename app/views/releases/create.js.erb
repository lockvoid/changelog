//

var newRelease = document.querySelector('#new_release');

newRelease.parentNode.replaceChild(createElementFromHTML('<%=j render "shared/new-release", project: @project %>'), newRelease);

// Show the release separator.

var separator = document.querySelector('.project__recent-releases-timeline');

separator.style.display = "block";

//

var newRelease = document.querySelector('#new_release');

newRelease.parentNode.insertBefore(createElementFromHTML('<%=j render "shared/release", release: @release %>'), newRelease.nextSibling);
