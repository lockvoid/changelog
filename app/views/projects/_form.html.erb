<%= form_with(model: project, local: true) do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="form-actions">
    <%= form.submit %>
  </div>
<% end %>
