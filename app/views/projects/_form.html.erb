<%= form_with(model: project, local: true) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="form-actions">
    <%= f.submit class: 'button' %>
  </div>
<% end %>
