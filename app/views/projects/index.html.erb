<h1>Projects</h1>

<div class="projects-cards">
  <% @projects.each do |project| %>
    <%= render 'project-card', project: project %>
  <% end %>

  <div class="project-cards__card">
      <%= render 'new-project-card' %>
  </div>
</div>

