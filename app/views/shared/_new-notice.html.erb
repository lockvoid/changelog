<% if release.notices.empty? %>
  <%= link_to "Spice up with popover notices 🌶", new_release_notice_path(release), remote: true %>
<% else %>
  <%= link_to "New notice", new_release_notice_path(release), remote: true %>
<% end %>